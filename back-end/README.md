Módulo WebSocket para Chat em Tempo Real (chat.gateway.ts)

O chat.gateway.ts é o componente central no NestJS (usando Socket.IO) responsável por gerenciar conexões bidirecionais e em tempo real para a tela de mensagens entre quem perdeu e quem encontrou um objeto.
